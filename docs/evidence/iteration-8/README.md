# Iteration 8 evidence

The [full gate](verification.log) passed **191 Android and 207 Mac tests** before the startup copy cleanup. The [final Swift run](swift-final.log) passed all **207 Mac tests** after that cleanup, and [Mac packaging](package-final.log) completed. Reviewed code and copy are frozen.

The [installation record](installed-builds.json) confirms the installed Android APK exactly matches the current build hash. The updated Mac bundle launched through computer use; its startup accessibility tree and screenshot were successfully captured. The screenshot was shown inline to the user; no saved image file is claimed here. Startup remained pending macOS Keychain access, and the phone remained `AWAITING_RECONNECT`. **This is not a connected-flow pass.** The process-recovery and reply trials below preceded these installations.

Mac startup now distinguishes restoration, saved-connection access, failures and a paired phone without claiming an active connection. Fake-outcome tests cover missing or invalid saved keys and stale startup publications. User-facing copy is nontechnical and describes any macOS permission request conditionally. These changes do not establish the cause of the earlier startup stall or a runtime recovery pass.

The phone service recovered after app-UID process termination in [both](process-recovery.json) [trials](bonjour-kill-trial.json), each observed at 1.95 seconds. The session nevertheless remained `AWAITING_RECONNECT`: [40.27 seconds after the first service recovery](session-after-restart-40.json) and at the [60.31-second observation in the second trial](bonjour-kill-result.json). Bonjour removal and return correlated with the second trial; they are discovery hints, not authenticated recovery evidence. A [controlled same-binary Mac restart](session-after-mac-restart.json) restored a READY baseline. **This was not automatic recovery**, and it does not prove clipboard delivery.

Native Notification Center reply was **not executed**. The [result](native-reply-result.json) records reply count and matched count **0**, with the fixture result aged 631 seconds against a 600-second route lifetime. Fixture posting is not reply acceptance. [Fixture cleanup](native-fixture-cleanup.json) confirms stop, owned-file removal, fixture uninstall and host-directory removal. [Final cleanup](cleanup.json) confirms the phone was **Dozing**, brightness/mode/timeout/stay-awake matched the [original settings](original-settings.json), the fixture and Mac fixture temporary directory were absent, and the Bonjour monitor was stopped. Shizuku was unchanged this iteration; its initial root mode was reported.

Acceptance drift remains open: the [previous checkpoint](../iteration-7/README.md) records the original checker exiting **2** before scoring because the device-constraints document differed from its frozen hash; original spec and matrix hashes matched. That historical freeze is preserved. Current-scope test results do not resolve that mismatch or establish native reply, call audio or automatic recovery acceptance. **No overall completion score is claimed.**

Published logs normalize local paths and trailing whitespace; task artifacts retain original build output.
