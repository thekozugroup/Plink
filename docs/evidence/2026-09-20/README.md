# Checkpoint 2 evidence — September 20, 2026

## Automated gate and bounded file-transfer result

The canonical gate, integration logs, capture summaries, and UI reports are published in this directory. The [verification manifest](verification-manifest.json) binds the tested source and binaries; the [artifact hashes](artifact-hashes.json) bind the published evidence. Unlinked policy-report filenames below remain local supporting records.

The canonical gate is [verification-2-final-rerun.log](verification-2-final-rerun.log), exit 0: 131 Android tests and 101 Swift tests pass (48 XCTest + 53 Swift Testing); Android lint reports 0 errors and 32 warnings. `FileTransferCoordinatorTest` uses `runBlocking` to avoid virtual-time versus real-I/O flakiness, without weakening assertions. These results verify the automated suite, not full user acceptance. File policy fixtures contain 145 shared cases, including 63 exact `rawEnvelope` cases. The focused policy reruns are recorded in `file-policy-android-2-rerun.log` and `file-policy-swift-2-rerun.log`; their two-test filtered runs are narrower than the canonical gate.

**Final encrypted-file run: bounded PASS, exit 0.** [Mac file-run log](files-mac-roundtrip.log) and [Android file-run log](files-android-roundtrip.log) record all ten transfers: 0, 1, 32,768, 32,769 and 16,777,216 bytes in both directions. The run exercised real file I/O, receiver saves, SHA-256/byte equality and private staging cleanup. This is automated Android-emulator/Swift harness evidence; it does not establish native picker completion or physical-device transfer. The 15-minute aggregate harness budget did not change production timeouts.

The [file capture summary](files-capture-summary.json) reports a keyless TCP relay forwarding the exact length-prefixed encrypted bytes: 1,052 frames per direction, maximum frame sizes 58,910 bytes Android-to-Mac and 58,903 bytes Mac-to-Android. It reports zero plaintext-marker matches, zero unexpected outer fields and no capture errors. Source/target device IDs, sequence and timestamp remain visible metadata. These checks do not prove that all sensitive content is absent or independently certify the cryptography. The [independent integration review](independent-integration-review.json) verifies all four raw capture hashes, frame counts, unique outer nonces, contiguous per-run sequences, fixture hashes, and source/binary bindings. No new P1/P2 issue was found in that bounded inspection. Task-owned raw captures were then [removed](raw-capture-cleanup.json); summaries and hashes remain. After the file pass, the isolated emulator exited for an unknown reason. Restarting the same task-owned AVD allowed the unchanged binaries to pass the reply regression. The [Mac reply log](replies-mac-roundtrip.log), [Android reply log](replies-android-roundtrip.log), and [reply capture summary](replies-capture-summary.json) record authenticated reply delivery, actual synthetic RemoteInput execution, one-time use, revocation, and acknowledgment. These results do not establish long-term emulator stability or real-recipient delivery. The [previous checkpoint](../2026-09-19/README.md) remains unchanged.

**Final tested APK UI:** the [capture report](verified-apk-ui.json) binds [Settings](verified-apk-settings-large-text.png) and [Connection](verified-apk-connection-large-text.png) screenshots to the final tested APK (`031d5196298d8a1cfd4da3aa4fd201af82191a8822e6c8528029911bbbe7b4d7`). Settings guidance wraps at 1.5× font scale. The Connection screen exposes a remaining defect: the Stop label clips to “Sto”. That defect is open for the next iteration. Font scale was restored to 1.0. These are unpaired-screen checks, not active transfer or physical-device acceptance.

**Earlier UI screenshots: available, with a bounded binary binding.** The [final UI report](ui-final-evidence-2.json) records corrected header clipping in [scrolled Settings](final-settings-large-text-scrolled.png), fully wrapped guidance in [large-text Settings](final-settings-large-text-top.png), and the idle [Files row](final-settings-files.png). These resolve the recorded title/content overlap for the captured states. They do not show an active or completed transfer.

The installed and local capture APKs were byte-identical at SHA-256 `5da77b790a0d93d5a95abe9da3c9368b7edaedf09ff282b168dfbee879091bfe`; `PlinkAppScreen.kt` was `1c198590ecd71a1cc9b696166add25fa1eae451a9d5f43e5a331d43ccacbe28e`. This APK predates the final URI-guard change. The UI source hash is unchanged, but these earlier screenshots are bound only to their recorded APK. The final-APK captures above provide separate evidence. Captures used 1.5× font scale and disabled animator duration; the recorded original settings were restored. The existing navigation video below remains earlier evidence; no new final video is claimed.

## Bounded file implementation and evidence limits

| Constraint | Implementation anchor | Evidence boundary |
| --- | --- | --- |
| Existing authenticated paired transport; Files defaults off; explicit consent | [Mac controller](../../../macos/Sources/PlinkMac/FileTransferController.swift), [Android coordinator](../../../android/src/main/java/app/plink/android/continuity/FileTransferCoordinator.kt) | Source, core tests and the ten-transfer encrypted harness pass; native consent/picker behavior remains unverified. No new unauthenticated transfer server. |
| 16 MiB file, 32 KiB chunk; exact fields, canonical base64 and bounded integers | [Swift policy](../../../macos/Sources/PlinkCore/FileTransferPayloadPolicy.swift), [Kotlin policy](../../../android/src/main/java/app/plink/android/protocol/FileTransferPayloadPolicy.kt), [145 shared vectors](../../../shared/protocol/v1/file-transfer/cases.json) | 63 raw cases preserve numeric lexemes. File numbers require unsigned integer tokens, at most eight digits plus field bounds; signs, fractions and exponents reject before typed decoding. Shared Unicode blank-name rules are explicit. This does not certify the entire transport. |
| One active transfer per peer; no resume; receiver acceptance before chunks; ordered progress acknowledgments | [Mac transfer engine](../../../macos/Sources/PlinkCore/MacFileTransfer.swift), [Android coordinator](../../../android/src/main/java/app/plink/android/continuity/FileTransferCoordinator.kt) | Core tests plus the bounded cross-platform transfer pass; this does not independently cover every failure/lifecycle path. Session replacement/disconnect/revocation cancels transfer. |
| Native selection and destination authority | [Mac controller](../../../macos/Sources/PlinkMac/FileTransferController.swift), [Android environment](../../../android/src/main/java/app/plink/android/continuity/AndroidFileTransferEnvironment.kt) | Mac `NSOpenPanel`/`NSSavePanel`; Android explicit shared-URI send confirmation and notification-bound `ACTION_CREATE_DOCUMENT` receive. No native end-to-end picker success inferred from engine tests. |
| Private staging; size/SHA-256 verification before export; saved result after output completion | [Mac engine](../../../macos/Sources/PlinkCore/MacFileTransfer.swift), [Android coordinator](../../../android/src/main/java/app/plink/android/continuity/FileTransferCoordinator.kt) | Peer display names do not choose output paths. No automatic open or execute. Provider failure/partial-output cleanup still requires its own evidence. Sender does not infer saved from bytes sent. |
| Bounded lifecycle | [Wire contract](../../../shared/protocol/v1/file-transfer.md) and both engines above | 60-second offer, 30-second inactivity, 300-second transfer limit. A longer aggregate test-harness budget does not change production timeouts. Transfers do not enter the durable notification outbox. |

The `file-policy-remediation-2.json` records the numeric, Unicode, slash-heavy chunk, JVM regex stack-overflow and trailing-comma fixes. Its author also implemented those changes; it is not independent certification. Independent review and evidence binding remain separate gates. The Mac session remains locked, confirmed through CUA; native Mac UI verification is blocked. Real carrier-call control, two-way laptop audio, native Notification Center replies to the correct conversation, and broader human acceptance remain unresolved. No completion percentage is inferred from test counts.

## Earlier Android UI captures retained for comparison

The [earlier UI report](ui-evidence-2.json) records these capture settings, hashes and limits.

This checkpoint contains real pixels from the isolated API 36 arm64 emulator `emulator-5580`. The installed `app.plink.android` base APK and the local debug APK were byte-identical, with SHA-256 `cad2f7cfb48e93df27dd74ecc61abc92717f8c98928e7c8c5ccdab9fc695c658`. The UI source under inspection, `PlinkAppScreen.kt`, had SHA-256 `55ab6a878cb4c8b0eb89454903b0383f7f43b9c1b8e7d24b848f7615ea4ee3d5`. Repository HEAD was `6bce1e6cc194862c3b4d364da794b93b36e361f8`; the UI correction was an uncommitted working-tree change at capture time.

The captures use Android `font_scale=1.5` and `animator_duration_scale=0`. `android-emulator-connection-large-text.png`, `android-emulator-activity-large-text.png`, and `android-emulator-settings-large-text-top.png` cover all three destinations. `android-emulator-reduced-motion-navigation.mp4` records navigation through Connection, Activity, and Settings with the animator duration scale disabled. It demonstrates immediate destination changes, not a formal timing or vestibular-accessibility study.

The prior Clipboard truncation is corrected in `android-emulator-settings-large-text-top.png`: “Use Android share to send, or tap an incoming Plink notification.” wraps in full without ellipsis. `android-emulator-settings-large-text-files.png` shows the newest Files integration row and its full status, “Sending works; incoming files require Pixel notifications.” The lower Settings captures show Media, SMS, Screen mirror, permission rows, and the final Refresh permissions control. Every text node reported by UIAutomator remained within the 1080×2400 display bounds across the captured states. These checks establish visual presence and scroll reachability; they do not establish human TalkBack order, permission behavior, file-transfer behavior, pairing, or physical-device accessibility.

Visual inspection found one separate issue. When Settings is scrolled, list content paints behind the fixed title/status region and visibly overlaps it near the top edge. The lower evidence intentionally preserves this behavior. The floating navigation also covers part of whichever row lies beneath it, but continued scrolling exposes the covered content and the final control. The title overlap was open at this capture and is corrected in the later screenshots linked above; no production source was changed during this evidence pass.

The unchanged Connection and Activity screens remain coherent at 1.5×, with no horizontal clipping in the inspected frames. This checkpoint only extends evidence for large text, reduced-motion navigation, the corrected supporting guidance, and the current Files row. It does not replace earlier light/dark screenshots or broaden their product-acceptance scope.

Before capture, emulator settings were `font_scale=1.0`, `animator_duration_scale=null`, `transition_animation_scale=1.0`, and `window_animation_scale=1.0`. Those exact values were restored afterward. No pairing, permission, feature toggle, Pixel, or external application was changed.

## Independent acceptance review

The [frozen-matrix review](adversarial-review.json) supports this development checkpoint but does not approve full product completion. Under its all-criteria-per-case scoring rule, 0 of 27 original cases are fully closed, so all nine original metric scores remain 0/100. These are complete-acceptance scores, not implementation-quality percentages. The report separately credits the passing build, ten bidirectional file saves, synthetic reply regression, and independent capture/source/binary checks. Physical calls and laptop speech, native Notification Center interaction, user-facing pairing and file pickers, accessibility, background recovery, and broader continuity features remain open. The next build should retain exact tool versions in its log; current evidence does not establish the versions at the earlier build time.
