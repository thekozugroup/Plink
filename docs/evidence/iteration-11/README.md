# Iteration 11: app icons and phone notification artwork

Native notification artwork remains unverified. The private data-icon attempt, all three comparison variants, the ordinary installed-app baseline, and the modern asset-catalog baseline rendered blank primary icons. The failed private-icon target, factory, retry path and unused Mac PNG decoder/badge renderer have now been removed. Notifications use ordinary delivery without attachments. Source-app/authenticated-phone display labels, reply routing and submission ownership/dismissal guards remain. Android optional metadata is unchanged; the Mac does not consume its icon PNG. The three-probe runtime code is also removed.

The final cleanup gate passed **452 bounded unit tests: 207 Android (unchanged) and 245 Swift**. The [final Swift log](swift-checkpoint-final.log) records 54 XCTest, 67 app Swift Testing and 124 core Swift Testing tests, exit 0. The [final release package](package-checkpoint-final.log) also exited 0 with strict signature verification. Parent installed this cleanup build at `/Applications/PlinkMac.app` after an idle check. The cleanup preserves ordinary submission ownership/cancellation/dismissal regressions and removes tests specific to deleted private-icon code.

The earlier **456 = 207 Android + 249 Swift** result remains historical, before that cleanup; [baseline Swift log](swift-assets-baseline.log). The still-earlier full gate passed 454. See [verification details](verification-result.json). These different counts reflect changed test scope, not failed tests. Passing tests, signing and installation do not establish native icon rendering. The source-app primary icon and phone badge remain unresolved; the prior CUA reply was verified on the baseline and was **not rerun after cleanup**.

The [final cleanup record](cleanup-final.json) records fixture removal and owned temporary-file cleanup; the last recorded display state was Dozing. Copied final logs and cleanup metadata replace the repository path with `<REPO>` and the user home with `<HOME>`.

## Native evidence

- The [initial right-side attachment](notification-before-layout.png) was rejected by the user. The later [private data-icon attempt](notification-left-data-failed.png) removed the right attachment but showed a blank left icon without a phone badge.
- The [three-way native comparison](notification-comparison-all-failed.png) showed blank icons for data, retained PNG path and own-bundle control. [Construction/readback and submission succeeded](icon-comparison-submission.json), but [native rendering failed](icon-comparison-visual.json). There was no positive icon control; a PNG-versus-TIFF explanation is not established.
- The ordinary baseline was installed at `/Applications/PlinkMac.app`. Launch Services records for both the source-build bundle and the Applications bundle persisted; neither was unregistered. It used neither a private icon override nor an attachment. The [user-provided screenshot at 2:02:14 PM](notification-plain-installed-failed.png) shows a blank primary icon; [baseline result](plain-baseline-visual.json). CUA later independently verified the same blank icon after a refresh; the linked image is the user’s screenshot. NSWorkspace resolving the correct blue-and-white app icon did not establish notification rendering.
- Before asset-catalog packaging, CUA successfully replied from Mac Notification Center with the exact synthetic `testword`; the local Android fixture recorded **count 1, matched 1**. The [exact reply record](plain-baseline-cua-reply.json) establishes this reply flow, not icon acceptance. Earlier native-pipe failures are historical and do not negate this successful CUA interaction.

The approved packaging experiment compiled the unchanged ICNS artwork into `Assets.car`, with `CFBundleIconName=AppIcon` and the existing ICNS fallback. The modern package built successfully with a 99,816-byte `Assets.car` and was installed; parent observed that experimental build as PID 93017 with session READY; this is historical, not the final cleanup build’s process identity. The [user-provided screenshot at 2:15:27 PM](notification-assets-installed-failed.png) confirms that this build also rendered a blank primary notification icon; [asset baseline result](assets-baseline-result.json). Source-app icon and phone-badge acceptance remain unresolved. These observations establish neither an OS defect nor that the private API is unsupported. Astra’s final decision removes the failed private machinery and retains standard asset-catalog packaging and the ICNS fallback. Retention is not a claim that packaging fixed the blank icon.

## Preserved scope and limits

The Mac and Android launcher icons retain the Lucide link with blue `#007AFF` and white artwork. Android's adaptive foreground was reduced to 75% around its center after a real circular launcher render exposed clipping. The [Mac preview](plink-macos.png), [Android launcher export](plink-android.png) and [rendered phone-badge PNG](notification-phone-badge.png) are not proof of native notification icon placement.

Android obtains optional source-app metadata through PackageManager under its own UID. The [corrected launcher fixture passed](android-artwork-device-final.log) with a bounded 96×96 PNG after the scoped launcher visibility query; [earlier failures](android-artwork-device.log) remain historical evidence. Non-launcher packages can still use text-only fallback. The historical Mac decoder/renderer produced the saved artwork preview; that unused production code has now been removed. Source app and authenticated phone subtitles remain separate from icon rendering.

The original orange fixture was historical; the current comparison uses blue artwork. The [fixture source archive](synthetic-fixture-source.tar.gz) excludes temporary signing material and compiled outputs. Replies are local synthetic fixture operations, not external messages. This checkpoint makes no new carrier-call or two-way Bluetooth audio claim, and no overall completion claim.

The final installed cleanup build remained at “Restoring your saved connection…” during the last check, with Android awaiting reconnect. Startup recovery code was unchanged in this checkpoint. Connection recovery and a fresh native reply on that installed build remain unverified.

Published logs normalize trailing whitespace and replace local home/repository paths. Raw logs remain in local orchestration artifacts.
